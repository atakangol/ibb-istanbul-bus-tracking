# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors / IDE
.idea/
.vscode/
*.swp
*.swo

# Secrets & local config
.env
.env.*
!.env.example
*.pem

# Python
__pycache__/
*.py[cod]
.venv/
venv/
ibb-bus-env/
.Python

# Node (if used later)
node_modules/

# Large local datasets (adjust paths as you add data)
data/raw/
# SQLite: static cache (stops, lines, kv_cache) + observation tables (vehicle_polls, stop_passage_events)
cache/*.sqlite
pi/data/*.sqlite
